export const TOKEN_NAME = 'token';

export enum NotificationStatusArg {
    SUCCESS = 'success',
    ERROR = 'error'
}

export enum ResponseStatus {
    FULFILLED = 'fulfilled',
    REJECTED = 'rejected'
}